# Sources

This pack is assembled from the local Codex skills directory on 2026-05-30.

Included sources are mixed:

- locally authored or adapted skills with no explicit `origin` field
- skills with `origin: ECC`
- skills with `origin: community`
- skills with `origin: ECC direct-port adaptation`

The generated `skills-inventory.json` lists every included skill directory, frontmatter name, description, and origin when available.

Before redistributing this repository outside the owner's intended audience, review each included skill's license and source permissions.
